#include <iostream>

#include "canvas.h"
#include "input.h"
#include "my_string.h"
#include "proud.h"

int main() {
    // HTMLInput input;
    // input.AddAtribute(new TypeText());
    // cout<<input.generate()<<endl;
    // process p;
    // p.AddToMap(1,new ProcessAVG);
    // DataIN din(1,125478455,{1,2,3,4,5});
    // DataIN din2(2,22222222,{1,3,4,5,88,99});
    // p.processData(din);
    // p.processData(din2);
    canvas c;
    c.addcell(new cell);
    return 0;
}
